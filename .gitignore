# Python caches / compiled artifacts
__pycache__/
*.pyc
*.pyo
*.pyd
*.so
*.abi3.so

# OS / editor
.DS_Store

# Secrets / API keys — NEVER commit. Read keys from environment variables
# (e.g. OPENAI_API_KEY) instead of files.
.env
.env.*
*.key
*.pem
secrets*
*_key.txt
openai_key*

# Local Claude Code settings (personal, machine-specific)
.claude/

# Vendored third-party libraries (~48M; includes platform-specific .so binaries
# that are not portable). Reconstruct all 5 libs into
# Ollama问题存在性实验/exec_oracle/vendor/ (see docs/PairCoder_Benchmark.md §3.3):
#   cd Ollama问题存在性实验/exec_oracle
#   python3 -m pip install --target vendor \
#     simplug==0.5.7 diot==0.3.4 python-simpleconf==0.9.3 glom==25.12.0 bidict==0.23.1
Ollama问题存在性实验/exec_oracle/vendor/
PairCoder/exec_oracle/vendor/
