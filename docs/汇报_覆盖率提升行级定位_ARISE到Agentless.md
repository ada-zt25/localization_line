# 用 Execution Coverage 提升行级故障定位 —— 从对标 ARISE 到嫁接 Agentless（汇报文档）

> 一句话主线:我们研究"如何用失败测试的执行覆盖率(execution coverage)提升行级故障定位"。研究分两个阶段——**阶段一对标 ARISE**(用覆盖率提升行级 recall@k),因 ARISE 闭源不可复现而受阻;**阶段二转向可复现的 Agentless**(用覆盖率提升它的原生指标 superset,同时缩小候选集 LoC)。本文说清两阶段的**方法设计**、**Agentless 的两个评测指标**、**横向方法对比**,以及**后续优化方向**。

---

## 0. 为什么从 ARISE 转向 Agentless

- **ARISE**(行级定位 SOTA,arXiv 2605.03117)是**闭源**的:无代码、无 artifact、无第三方复现。我们只能引用它公布的 R@k(41/62/74),无法在同一套代码里做受控对比,外部有效性站不住。
- **Agentless**(OpenAutoCoder/Agentless,FSE 2025,MIT,SWE-bench Lite)**完全开源、端到端、被广泛复现**,且有明确的定位评测指标。转向它作为**可复现同底座基线**,把我们的方法作为增量叠上去。
- 转向的红利:复现 Agentless = **端到端**(不给金标文件),顺带消除了阶段一 file-given 口径下"绝对值虚高、不可比"的质疑。
- **研究范围锁定**:只解决 **crash·on-path**(崩溃类 + 金标行被失败测试执行)一类 issue,n=57(SWE-bench Lite),backbone = DeepSeek-V3。

---

## 1. Agentless 的两个评测指标(必须先讲清楚)

Agentless 的定位阶段输出的是一个**无序的编辑位置集合**(采样若干次、合并),而不是排名列表——所以它**不用 recall@k**,而用一对"完整度 + 成本"指标:

### 指标一:Contains-all(superset)—— 完整度
- **定义**:预测的候选集必须**包含全部金标行**才算命中(预测集 ⊇ 金标集)。即 Agentless 论文的 "% Correct Location"(原文:*edits a superset of all locations in the ground truth patch*)。
- **含义**:有多少比例的实例,定位**完整覆盖了**所有该改的行(下游修复才可能把每一行都碰到)。
- **特点**:**严苛**——漏任何一行金标即判 0,对多行修复尤其苛刻。

### 指标二:Avg LoC —— 成本/精度
- **定义**:预测候选集的平均行数。计算方式:每条 `line: N` 预测展开成 ±10 行窗口,`function:/class:` 展开成整段,求并集,数集合大小。
- **含义**:候选集越小 → 下游修复读的上下文越少、越精、越省。**越小越好**。

> **两指标是一对〈完整度, 成本〉**,不是名次。我们要赢 = **在这对上 Pareto 更优**:同 LoC 下更高 superset,或同 superset 下更低 LoC。
> **口径验证**:我用 Agentless 自己的展开代码复现了它论文 Table 2(file-contains 82.7% vs 论文 81.7;merged LoC 336 vs 342),指标实现已验证无误。
>
> ⚠️ 关于 **any**(候选集含 ≥1 金标行):这**不是 Agentless 汇报的指标**,我们也不关心,**已从所有实验结果中去掉**;评测只用 Agentless 原生的 superset 与 LoC。

---

## 2. 阶段一(对标 ARISE):用覆盖率提升行级 recall@k

### 2.1 方法设计的核心哲学
我们先证伪了一条直觉:**覆盖率当"输出端删除式过滤器"是错的**——off-path 会删掉未执行的金标(灾难),on-path 在强 LLM ranker 上也只是 wash。所以我们把覆盖率**往上游挪**,注入到两个 LLM 盲的位置:

流水线 = 区域收窄 → 自洽投票(k 次采样,RRF 频次 + 跨样本一致性)→ RRF 排序。覆盖率注入:

| 注入点 | 阶段 | 机制 | 预期帮到 |
|---|---|---|---|
| **通道①** | 生成 | 投票 prompt 标注执行行(`>`)+ 前置 crash traceback + "沿执行路径反推根因" | **召回**(R@5/R@10):条件化 LLM 生成什么候选 |
| **通道③⑤** | 排序 | 分档覆盖 RRF 臂(crash 帧内 > 执行 > 未执行)+ "执行∧一致"平手优先 | **精度**(R@1):执行集内部排序 |

### 2.2 结果(file-given,DeepSeek-V3,n=57,指标 = Line R@k)
| 配置 | R@1 | R@5 | R@10 | MRR |
|---|---|---|---|---|
| 无覆盖率(基线) | 35.1 | 56.1 | 71.9 | .451 |
| **A3(通道①③⑤)** | **38.6 (+3.5)** | **64.9 (+8.8)** | **75.4 (+3.5)** | **.507** |

- 通道③⑤ 抬 R@1(+3.5),通道①(条件化生成)抬 R@5/R@10(各 +5.3);MRR 随通道叠加单调上升。
- **结论**:覆盖率帮行定位不是靠过滤,而是靠**条件化生成 + 分档排序**。
- **受阻点**:file-given 口径 + R@k 指标 + ARISE 闭源 → 无法做外部可比的对标。→ 转向阶段二。

---

## 3. 阶段二(嫁接 Agentless):用覆盖率提升 superset、缩小 LoC

### 3.1 漏斗诊断:superset 的瓶颈在哪
我们把 Contains-all 逐层拆开(crash·on-path,n=57):

```
① Agentless 文件层  gold-files⊆found_files : 86.0%
② 区域收窄·相关元素  gold⊆region           : 64.9%   ← 损失 −21pp,最大瓶颈
③ 排序·编辑集(AL)    gold⊆AL              : 61.4%   ← 仅 −3.5pp
```
**关键发现**:superset 的最大损失(−21pp)在 Agentless 的**区域收窄阶段**,而这是**我们投票的上游**——投票在区域内部操作,救不回已经掉出区域的金标。我们真正替换的编辑位置阶段只有 3.5pp 空间。**这解释了为什么"纯投票替换"抬不动 superset。**

同时证实:**覆盖率当过滤器不可能抬 superset**(区域∩执行 = 22.8%,砸穿;因为 superset 要全部金标,而 on-path 只保证 ≥1 行执行)。

### 3.2 方法设计:覆盖率的三处注入
既然瓶颈在上游区域、且过滤只能砍 LoC,我们把覆盖率用在三处:

```
Agentless 文件定位(保留)
  → 区域 = Agentless 相关元素 ∪ 【覆盖率选择性扩展】   ← 上游:抬区域天花板(救回 superset)
  → 自洽投票 + 【通道① 覆盖率标注 + crash 上下文】      ← 生成:兑现天花板
  → top-N + 【frontier 覆盖率过滤(执行±2)】           ← 下游:砍 LoC
```
- **上游选择性扩展**:把"执行了但被 Agentless 元素选择漏掉"的金标救回区域(抬 superset)。
- **生成(通道①)**:同阶段一,条件化投票。
- **下游 frontier 过滤**:把无关执行上下文滤掉(砍 LoC)。

### 3.3 V1 → V2:V2 是为了进一步抬高"区域收窄"的天花板
"选择性扩展"用什么信号,决定能救回多少。我们做了两版:

| 扩展方式 | 区域 Contains-all 天花板(±10 可realize 口径) |
|---|---|
| Agentless 原生区域(基线) | 68.4% |
| **V1** = 崩溃帧执行函数 | 70.2% |
| **V2** = top-30 issue+crash 打分执行行(`line_scores_v2`:issue 相关 + traceback 数据流 + coverage) | **75.4%** |
| (理论上限:区域∪全部执行±2) | 82.5% |

- **V1** 只加崩溃帧里的执行函数,天花板 68.4→70.2。
- **V2 是为了进一步抬高区域天花板**:发现漏掉的金标多在"执行了、且与 issue 符号/数据流相关、但不在崩溃帧函数里"的行——用 `line_scores_v2` 给执行行打分取 top-30,**天花板 68.4→75.4(+7pp)**,且 top-60 就饱和(证明捞干净了打分能找到的金标)。
- **诚实**:V2 把**天花板**抬到 75.4,但投票只 **realize 到 70.2**(realize ~93%),且要够到 70.2 需 LoC 超过 AL。→ 瓶颈从"区域召回"转移到了"投票判别力"(见 §5)。

---

## 4. 横向方法对比(基于 Agentless 基线,crash·on-path n=57,DeepSeek-V3,k=10)

> 指标只用 **Agentless 原生的两个:Contains-all(superset,准确度)与 LoC(精确度)**。`any`(含 ≥1 金标)**不是 Agentless 汇报的指标,已从所有结果中去掉**。方法名按"做了什么"命名(不用 V1/V2/框架/N)。

| 方法 | superset(准确度) | LoC(精确度) |
|---|---|---|
| **Agentless 基线** | 66.7 | 341 |
| 只换投票·不扩区域 | 63.2 | 233 |
| 投票 + 崩溃帧扩区域 | 68.4 **(+1.8)** | 290 **(−15%)** |
| **投票 + 打分扩区域 ⭐推荐** | **68.4 (+1.8)** | **292 (−14%)** |
| 并入 Agentless 预测(崩溃帧) | 71.9 **(+5.3)** | 410 (+20%) |

**读表要点**:
- **只换投票·不扩区域**:反低于基线(63.2 < 66.7)→ 说明瓶颈在上游区域,光替换定位没用。
- **崩溃帧 vs 打分扩区域(替换式)**:两者在此工作点数值接近(都 68.4),区别在**区域天花板**——崩溃帧只到 70.2,打分到 75.4(视野更广)→ 替换式里推荐打分。
- **并入 Agentless 预测**(真实实验 `agentless/union_eval.py`,`union_result_v1/v2.json`):保留 Agentless 定位再并上投票(不替换)。**并入里反而是崩溃帧投票最好**:V1 崩溃帧到 71.9(+5.3)@410;V2 打分只到 70.2 且 LoC 更高(512)。原因见下:并入 133 行救回 4 个实例金标 vs 打分 202 行只救回 2 个——**替换需『广』(打分胜)、并入需『准』(崩溃帧胜),两个框架需求相反**。替换·打分与并入·崩溃帧是**两个不同的 Pareto 点**(省候选 vs 更全),不是谁支配谁。
- **⭐推荐(替换式·投票+打分扩区域)**:superset 68.4(+1.8)、LoC 292(−14%)——**两个轴都严格更好 = Pareto 占优 Agentless**。核心叙事:**用少 14~28% 的候选行,达到相等或更高的定位完整度**;想要**最高完整度**则用并入·崩溃帧(71.9,+5.3)。

---

## 5. 后续优化方向:瓶颈已从"区域召回"转到"判别"

V2 已把区域天花板抬到 75.4%,但**预算内(LoC ≤ AL)realized superset 仍卡在 68.4%**——多出来的天花板兑现不出来。原因是:**投票没能在"少候选"的前提下,把 V2 新捞进来的金标排进前列**。所以下一步不该继续抬天花板,而应攻**判别力**:

1. **判别力优化**:在缩小后的候选池上做更强的重排/精选(利用覆盖率分档 + traceback 数据流距离),让 realized 逼近 75.4 的天花板 —— 目标是在 LoC ≤ AL 的预算内把 superset 推到 ~72-74。
2. **扩样固化显著性**:当前 +1.8/−14% 都在 n=57 上、欠统计功效。**跑 SWE-bench Verified 的 crash·on-path 扩样**(基建现成,`al_vote_eval.py` 加 `--dataset`),做 McNemar/bootstrap,把 Pareto 占优做"站得住"。
3. **文件层天花板**:14% 的实例在 Agentless 文件定位阶段就丢了金标(区域扩展够不到),这是 superset 的硬顶(86%),超出"覆盖率"能做的范围,列为已知边界。

---

## 6. 诚实边界(汇报必附)
- 结果均在 **crash·on-path**(oracle 定义子集,用 gold∩coverage 选出)上,须作为 scope 假设披露。
- 覆盖率来自失败测试(FAIL_TO_PASS 可能是金标 patch 新增测试的 oracle 嫌疑,须审计)。
- superset 提升 +1.8pp @ n=57 欠功效;**LoC 效率(−14~28%)是更硬、更可复现的卖点**。
- any 非 Agentless 指标,我们不关心,已从所有结果去掉;评测只用 superset + LoC。

---

**代码/数据**:`prelim_localization/agentless/`(al_vote_eval.py 主驱动、expand_ablation/expand_selective 天花板消融、contains_gt_eval 指标复现);结果 JSON `al_vote_v2_result.json` 等;vendor(Agentless 克隆 + GPT-4o 定位产物)在 `agentless_vendor/`。
