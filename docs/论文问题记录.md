 【需要解决的问题】
1. 设计的几类函数关系是否全面覆盖？扩充benchmark。
2. 100%，83%等数字是否合理？背后分层机制等需要引入到试验中。
3. 有没有可能和最新的sota比较？
4. 成果的milestone等设计？目标期刊选择？投稿节点？
5. 创新点凝练一下？

## A. 必须补的实验(要开卡)— 决定能不能投

| # | 缺的数据 | 现状 | 为什么是硬伤 |
|---|---|---|---|
| **A1** | **完整流水线 ± coverage-filter 的 +14pp** | RQ2/RQ3 **全部**是离线 def-use、file-given、无 LLM(§3.3 已自承 `〔GPU待跑〕`) | 主结论从未在真实方法上跑过。脚本已备:`EXPERIMENTS.md` 的 RQ3‑E4~E8 |
| **A2** | **我们系统自己的端到端 line R@{1,5,10}**(与 ARISE 41/62/74 同口径) | 论文显著引用了 ARISE 的 41/62/74,却**没有一张我们自己的同口径表** | 审稿人必问"你的 baseline 是多少?"。至少要:static-only 一行(应≈ARISE 量级)+ +filter 一行,n=300 |
| **A3** | **跨 backbone 复现** | RQ3 只在单一离线 ranker;RQ1 用 Qwen2.5‑Coder‑32B,realism 轴用 DeepSeek‑V3 | "+14pp"现在是单点。至少 Qwen‑32B + DeepSeek‑V3 两个 |
| **A4** | **SBFL‑null 在真 LLM ranker 上复核** | 只在离线 def-use 上验证 | 否则"SBFL 无增益"可能被质疑是离线 ranker 的伪影 |

---

## B. 现有数据的核对 / 自洽(不一定要算力,但必须改)

**B1. crash/behavioral 与 on/off‑path 是 regex 代理**(§3.5 已承诺),要用 Docker 里真实 FAIL_TO_PASS traceback 复核。

**B2. 实例划分对不上 —— 必须补一张"300 → 子集"的流向表并核对 n。** 我实际查到的不一致:
- behavioral·on‑path:**论文 Table 5 写 131,子集文件 `rq3_subsets.json` 是 132**(差 1)。
- crash 总数三处打架:**Table 1 = 77**,RQ2.3 = **68**(57/68),子集文件 = **75**(57+11+7)。
- behavioral:Table 1 = **223**,但有覆盖的只有 **172**(132+40)→ 51 例去哪了没交代。
- 四子集合计 ≈ **239–240**,覆盖缓存 **258**,全集 **300** —— 中间两次"掉人"都没说明。

**B3. Table 7 的 n=37 与 Table 5 crash on‑path 的 n=57 不一致且无解释**(应是只有 37 例有可用 traceback)—— 至少加一句话。

**B4. 全文没有任何统计显著性 / 置信区间。** +14pp 建立在 n=57 上、SBFL"R@k identical"建立在 n=9 上,都需要 McNemar 检验或 bootstrap CI,否则"+14pp""identical"站不住。

---

## C. 样本太小,结论要么扩样要么明确缩 scope

- **C1. SBFL 研究 n=9、仅 4 仓库**,且 django/sympy 因 `PASS_TO_PASS` 不是 pytest 格式被排除 → 要么补采集 django/sympy 的通过覆盖,要么在 Threats 里把 scope 写死。
- **C2. off‑path n=11**(6 漏分支 / 4 插入 / 1 函数未达)—— 单数据点级别。
- **C3. realism 轴(Table 1b)只有 DeepSeek‑V3、n=40/40/20** 单 backbone,撑"realism"这一论点偏薄。

---

## D. 叙述里 promise 了但没给的小数据(加强项)

- **fraction recall**(多行修复全召回)在 RQ1 提到却没单独成表。
- 摘要/RQ3.2 的 **"precision 约翻倍"是从 recall+候选数推算**的,最好直接给 precision@k(或候选归一化的命中密度)。

---

## E. 文献尾巴(基本已核完)

- 仅两条预印本 venue 待终核:`orcaloca`(ICML 2025?)、`dissecting`(ICSE‑SEIP 2026?)。
- 可选:`sweagent` 作者表/venue 再确认(之前置信度 medium)。

---

### 一句话总结
**真正卡脖子的只有 A 类**:① 完整流水线坐实 +14pp、② 我们自己的端到端 line R@{1,5,10} 对标表、③ 跨 backbone —— 这三件都需要开卡,脚本已备好(RQ3‑E4~E8)。**B 类(划分流向表 + n 自洽 + 显著性)不用算力,是审稿人最容易一眼挑出的,建议先改。**

要不要我现在就**先把不需要算力的 B2/B3 做掉**——补一张实例划分流向表、把 131/132 和 n=37 的注解修正自洽?这能立刻消掉几个最显眼的审稿雷点。开卡时我再按 RQ3‑E4~E8 跑 A 类。